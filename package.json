{
  "name": "content-moderation-api",
  "version": "1.0.0",
  "description": "AI-powered content moderation API with multi-provider fallback",
  "main": "backend/src/index.js",
  "scripts": {
    "start": "npm run start --prefix backend",
    "dev": "node dev.js",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "install-deps": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "build:full": "npm run install-deps && npm run build",
    "render-build": "npm run build:full"
  },
  "keywords": ["moderation", "ai", "content", "api", "saas"],
  "author": "",
  "license": "ISC"
}
